# TODO

## Now

* explanation to use cwm and isotop specific configuration : menus, key,
  mouse...
* choose ports wisely 
* FAQ to answer some specific needs: smb, cups ?
* pixup copy url : xmessage ? xedit ? autre ?
* port isotop-tools : .tgz ? 
* write doc to install another wm
* Doc for included scripts (optimize, setwall...)
* Add custom actions in pcmanfm (rotate image, resize...) https://wiki.lxde.org/en/PCManFM#Run_as_root
* documenter scripts dmenu...
* add .desktop files


## Later

* Rewrite python tools into tcl/Tk 
* keep ffom ?
* gdoas safe ?
* opama cli ? (+ colors)

* Add tray for owi ?
* owi : add auto-join
* edbrowse for blind users ?
* use solene's repos instead of openup
