#!/bin/sh
# take screenshot with GraphicsMagick
# : $1 : name of the image
# : $2 : delay in seconds

if [ $# -lt 1 ]; then
	echo "usage : scrot <image-name> <delay (optional)>" 
	exit
fi

if [ -z "${2}" ]; then
	DELAY=0
else
	DELAY=${2}
fi
sleep ${DELAY}
gm import -window root "${1}.png" 
