#!/bin/sh
# download openup and run it

if [ ! -f /usr/local/bin/openup ]; then
    doas ftp -o /usr/local/bin/openup https://stable.mtier.org/openup 
    doas chmod +x /usr/local/bin/openup
    doas openup
else
    doas openup
fi

exit 0
